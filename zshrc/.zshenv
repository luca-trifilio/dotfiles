# Read by every zsh, interactive or not — unlike .zshrc, which non-interactive
# shells skip. Keep this file minimal: it runs for scripts and subshells too.

[[ -r ~/.config/zsh/secrets.zsh ]] && source ~/.config/zsh/secrets.zsh
